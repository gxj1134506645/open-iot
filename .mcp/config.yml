# Open-IoT 项目级 MCP 配置
# 用于 Claude Code 项目级 MCP 服务器配置

mcpServers:
  # PostgreSQL 数据库 MCP 服务器
  postgres:
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-postgres"
      - "postgresql://openiot:openiot123@localhost:5432/openiot"
